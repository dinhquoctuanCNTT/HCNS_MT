import { StyleSheet, Dimensions, Platform } from "react-native";

const { width } = Dimensions.get("window");
export const H_PAD = 16;
export const CELL_W = Math.floor((width - H_PAD * 2) / 7);

export const COLORS = {
  primary: "#0EA5E9",
  primaryDark: "#0284C7",
  primaryLight: "#E0F2FE",
  blue: "#2563EB",
  blueLight: "#DBEAFE",
  success: "#16A34A",
  successLight: "#DCFCE7",
  warning: "#D97706",
  warningLight: "#FEF3C7",
  danger: "#EF4444",
  dangerLight: "#FEE2E2",
  green: "#22C55E",
  bg: "#F5F5F5",
  white: "#FFFFFF",
  textDark: "#111827",
  textMid: "#374151",
  textSub: "#4B5563",
  textLight: "#9CA3AF",
  border: "#E5E7EB",
  borderLight: "#F3F4F6",
  red: "#EF4444",
};

export default StyleSheet.create({
  container: { flex: 1, backgroundColor: COLORS.bg },

  header: {
    backgroundColor: COLORS.primary,
    paddingTop: Platform.OS === "ios" ? 52 : 36,
    paddingHorizontal: H_PAD,
    paddingBottom: 0,
  },
  headerTop: {
    flexDirection: "row",
    alignItems: "center",
    marginBottom: 12,
    gap: 8,
  },
  backBtn: { fontSize: 22, color: COLORS.white, fontWeight: "300" },
  headerTitle: {
    fontSize: 18,
    fontWeight: "700",
    color: COLORS.white,
    flex: 1,
    textAlign: "center",
  },

  tabs: { flexDirection: "row", backgroundColor: COLORS.primary },
  tabItem: { flex: 1, paddingVertical: 10, alignItems: "center" },
  tabActive: { borderBottomWidth: 2.5, borderBottomColor: COLORS.white },
  tabInactive: { borderBottomWidth: 2.5, borderBottomColor: "transparent" },
  tabTextActive: { fontSize: 13, fontWeight: "700", color: COLORS.white },
  tabTextInactive: {
    fontSize: 13,
    fontWeight: "400",
    color: "rgba(255,255,255,0.65)",
  },

  metaCard: {
    backgroundColor: COLORS.white,
    paddingHorizontal: H_PAD,
    paddingVertical: 10,
    borderBottomWidth: 1,
    borderBottomColor: COLORS.border,
  },
  metaShift: { fontSize: 12, color: COLORS.textSub, marginBottom: 4 },
  metaShiftVal: { fontWeight: "600", color: COLORS.textDark },
  metaPeriodRow: {
    flexDirection: "row",
    alignItems: "center",
    justifyContent: "space-between",
  },
  metaPeriodLabel: { fontSize: 12, color: COLORS.textLight },
  metaDropdown: {
    flexDirection: "row",
    alignItems: "center",
    gap: 4,
    backgroundColor: COLORS.primaryLight,
    borderRadius: 6,
    paddingHorizontal: 10,
    paddingVertical: 4,
  },
  metaDropdownText: { fontSize: 12, color: COLORS.primary, fontWeight: "600" },

  calWrap: {
    backgroundColor: COLORS.white,
    paddingHorizontal: H_PAD,
    paddingBottom: 8,
  },

  yearNavRow: {
    flexDirection: "row",
    alignItems: "center",
    justifyContent: "center",
    gap: 16,
    paddingTop: 10,
    paddingBottom: 4,
  },
  yearNavText: { fontSize: 20, color: COLORS.textLight, padding: 4 },
  yearText: {
    fontSize: 15,
    fontWeight: "700",
    color: COLORS.textDark,
    minWidth: 50,
    textAlign: "center",
  },

  monthPillsContent: { paddingHorizontal: 2, gap: 6, paddingVertical: 4 },
  monthPill: {
    paddingHorizontal: 12,
    paddingVertical: 6,
    borderRadius: 20,
    backgroundColor: COLORS.borderLight,
  },
  monthPillActive: { backgroundColor: COLORS.primary },
  monthPillText: { fontSize: 12, color: COLORS.textLight, fontWeight: "500" },
  monthPillTextActive: { color: COLORS.white, fontWeight: "700" },

  summaryBadges: { flexDirection: "row", gap: 8, marginBottom: 10 },
  badgeCard: { flex: 1, borderRadius: 10, padding: 10, alignItems: "center" },
  badgeCardGreen: { backgroundColor: COLORS.successLight },
  badgeCardAmber: { backgroundColor: COLORS.warningLight },
  badgeCardRed: { backgroundColor: COLORS.dangerLight },
  badgeNum: { fontSize: 22, fontWeight: "800" },
  badgeNumGreen: { color: COLORS.success },
  badgeNumAmber: { color: COLORS.warning },
  badgeNumRed: { color: COLORS.danger },
  badgeLabel: { fontSize: 10, color: COLORS.textSub, marginTop: 2 },

  monthNavRow: {
    flexDirection: "row",
    alignItems: "center",
    justifyContent: "space-between",
    paddingVertical: 8,
  },
  monthNavBtn: { fontSize: 20, color: COLORS.textLight, padding: 4 },
  monthLabel: { fontSize: 14, fontWeight: "700", color: COLORS.textDark },

  dowRow: { flexDirection: "row", marginBottom: 4 },
  dowCell: { width: CELL_W, alignItems: "center", paddingVertical: 4 },
  dowText: { fontSize: 11, fontWeight: "600", color: COLORS.textLight },
  dowTextRed: { fontSize: 11, fontWeight: "600", color: COLORS.red },

  weekRow: { flexDirection: "row", marginBottom: 2 },
  calCell: { width: CELL_W, alignItems: "center" },

  dayCellWrap: {
    width: CELL_W,
    height: 36,
    alignItems: "center",
    justifyContent: "center",
  },

  dayNumDefault: { fontSize: 14, fontWeight: "500", color: COLORS.textDark },
  dayNumRed: { fontSize: 14, fontWeight: "600", color: COLORS.red },
  dayNumAmber: { fontSize: 14, fontWeight: "600", color: COLORS.warning },
  dayNumMuted: { fontSize: 14, fontWeight: "400", color: COLORS.textLight },
  dayNumFuture: { fontSize: 14, fontWeight: "400", color: "#D1D5DB" },

  todayCircle: {
    width: 26,
    height: 26,
    borderRadius: 13,
    backgroundColor: COLORS.primary,
    alignItems: "center",
    justifyContent: "center",
  },
  todayNum: { fontSize: 14, fontWeight: "700", color: COLORS.white },

  cornerDot: {
    position: "absolute",
    top: -2,
    right: -5,
    width: 7,
    height: 7,
    borderRadius: 3.5,
  },

  legend: {
    flexDirection: "row",
    gap: 16,
    paddingTop: 8,
    paddingBottom: 4,
    justifyContent: "center",
  },
  legendItem: { flexDirection: "row", alignItems: "center", gap: 5 },
  legendDot: { width: 8, height: 8, borderRadius: 4 },
  legendText: { fontSize: 11, color: COLORS.textSub },

  summaryBar: {
    flexDirection: "row",
    gap: 8,
    paddingHorizontal: H_PAD,
    paddingVertical: 10,
    backgroundColor: COLORS.white,
    borderTopWidth: 1,
    borderTopColor: COLORS.border,
  },
  summaryChip: {
    flex: 1,
    borderRadius: 20,
    paddingVertical: 6,
    alignItems: "center",
    justifyContent: "center",
  },
  summaryChipText: { fontSize: 11, fontWeight: "700", color: COLORS.white },

  listSection: {
    backgroundColor: COLORS.white,
    marginTop: 8,
    paddingHorizontal: H_PAD,
    paddingTop: 12,
    paddingBottom: 80,
  },

  logCard: {
    flexDirection: "row",
    backgroundColor: COLORS.white,
    borderRadius: 10,
    borderWidth: 1,
    borderColor: COLORS.border,
    overflow: "hidden",
    marginBottom: 8,
  },
  logLeft: {
    width: 52,
    alignItems: "center",
    justifyContent: "center",
    paddingVertical: 10,
    backgroundColor: COLORS.successLight,
  },
  logDay: { fontSize: 20, fontWeight: "700" },
  logDow: { fontSize: 9, fontWeight: "600", marginTop: 1 },
  logRight: { flex: 1, padding: 10 },
  logTimeRow: {
    flexDirection: "row",
    alignItems: "center",
    gap: 6,
    marginBottom: 6,
  },
  logTimeItem: { alignItems: "center" },
  logTimeLabel: { fontSize: 9, color: COLORS.textLight, marginBottom: 2 },
  logTimeIn: { fontSize: 15, fontWeight: "700", color: COLORS.success },
  logTimeOut: { fontSize: 15, fontWeight: "700", color: COLORS.blue },
  logTimeDash: { fontSize: 15, fontWeight: "700", color: COLORS.border },
  logDivider: { flex: 1, height: 1, backgroundColor: COLORS.borderLight },
  logTagRow: { flexDirection: "row", gap: 5, flexWrap: "wrap" },
  logTag: { borderRadius: 5, paddingHorizontal: 7, paddingVertical: 2 },
  logTagText: { fontSize: 9, fontWeight: "600" },

  explainSection: {
    backgroundColor: COLORS.white,
    marginTop: 8,
    paddingHorizontal: H_PAD,
    paddingTop: 12,
    paddingBottom: 80,
  },
  explainSectionTitle: {
    fontSize: 13,
    fontWeight: "700",
    color: COLORS.textDark,
    marginBottom: 10,
  },
  explainCard: {
    flexDirection: "row",
    alignItems: "center",
    backgroundColor: COLORS.white,
    borderRadius: 10,
    borderWidth: 1,
    borderColor: COLORS.border,
    padding: 12,
    marginBottom: 8,
    gap: 10,
  },
  explainDateBox: {
    width: 44,
    height: 44,
    borderRadius: 10,
    alignItems: "center",
    justifyContent: "center",
  },
  explainDateNum: { fontSize: 18, fontWeight: "800" },
  explainDateDow: { fontSize: 8, fontWeight: "600", marginTop: 1 },
  explainInfo: { flex: 1 },
  explainReason: {
    fontSize: 13,
    fontWeight: "600",
    color: COLORS.textDark,
    marginBottom: 3,
  },
  explainTime: { fontSize: 11, color: COLORS.textLight },
  explainBtn: {
    borderRadius: 8,
    paddingHorizontal: 10,
    paddingVertical: 6,
    backgroundColor: COLORS.primaryLight,
  },
  explainBtnText: { fontSize: 11, fontWeight: "700", color: COLORS.primary },

  detailHeader: {
    backgroundColor: COLORS.white,
    paddingTop: Platform.OS === "ios" ? 52 : 36,
    paddingHorizontal: H_PAD,
    paddingBottom: 14,
    borderBottomWidth: 1,
    borderBottomColor: COLORS.border,
    alignItems: "center",
  },
  detailHeaderRow: {
    flexDirection: "row",
    alignItems: "center",
    justifyContent: "space-between",
    width: "100%",
    marginBottom: 6,
  },
  detailNavBtn: { fontSize: 18, color: COLORS.textLight, padding: 4 },
  detailTitle: { fontSize: 16, fontWeight: "700", color: COLORS.textDark },
  detailDate: {
    fontSize: 14,
    fontWeight: "600",
    color: COLORS.textMid,
    marginBottom: 8,
  },
  detailStatusBadge: {
    flexDirection: "row",
    alignItems: "center",
    gap: 5,
    backgroundColor: COLORS.bg,
    borderRadius: 20,
    paddingHorizontal: 12,
    paddingVertical: 5,
  },
  detailStatusDot: { width: 7, height: 7, borderRadius: 4 },
  detailStatusText: { fontSize: 12, fontWeight: "500" },
  detailRow: {
    flexDirection: "row",
    alignItems: "center",
    justifyContent: "space-between",
    paddingHorizontal: H_PAD,
    paddingVertical: 13,
    borderBottomWidth: 1,
    borderBottomColor: COLORS.borderLight,
    backgroundColor: COLORS.white,
  },
  detailRowSub: { backgroundColor: COLORS.bg },
  detailRowIndent: { paddingLeft: 32 },
  detailLabel: { fontSize: 14, color: COLORS.textSub },
  detailLabelBold: { fontSize: 14, fontWeight: "700", color: COLORS.textDark },
  detailLabelSub: { fontSize: 13, fontWeight: "600", color: COLORS.textMid },
  detailValue: { fontSize: 14, fontWeight: "500", color: COLORS.textDark },
  detailValueMuted: { fontSize: 14, color: COLORS.textLight },
  detailCTA: { padding: H_PAD, paddingTop: 20, paddingBottom: 32 },
  detailCTABtn: {
    backgroundColor: COLORS.primary,
    borderRadius: 12,
    padding: 15,
    alignItems: "center",
  },
  detailCTAText: { fontSize: 15, color: COLORS.white, fontWeight: "600" },

  empty: { alignItems: "center", paddingTop: 48 },
  emptyText: { fontSize: 13, color: COLORS.textLight, marginTop: 10 },
});
